# 任务包 002 SPEC-001 v2 重写草案

## 任务目标

起草 `sih-engine/doc/spec/SPEC-001-divergence-experiment.md` 新文件,承接 DES-005 v2 偏离率收敛方法学与 DES-010 单 KEY 多专家范式实验设计,定义 SPEC-001 v2 实验协议。当前 SPEC-001 仍是 v1 N 等于 4 采样设计,与 OQ-13 N 等于 4 废止决议不一致。

## 项目背景

SPEC-001 是 v1 路径二验收依据,基于 N 等于 4 隔离采样加多数投票加 tie-break(SPEC-003 第 121 行)。N 等于 4 范式已废,OQ-13 登记,路径二改为偏离率收敛(DES-005 v2)与单 KEY 多专家(DES-010)。SPEC-001 v2 须重写,实验协议改基于偏离率 K 由 N 除加 Jaccard 相似度加收敛双指标。

## 任务构成规则

### self-check(六项核心)

1. 主动判断产出性质 — 这是 SPEC 重写,不是 v1 修订
2. 服务原始意图 — 重写实验协议,不发明新度量机制
3. 范畴排除显式 — 接受 DES-005 v2 与 DES-010,不另立
4. 不逃避当下责任 — 阈值留空,等实验数据填入
5. 每行去掉会犯错吗 — 实验协议需可机械执行
6. 与哲学仓相容 — 承接 PRO-08 应而不藏

### 通用格式规范

遵守 DES-001 通用格式规范。SPEC 文档结构含接口签名加数据契约加验收依据加度量权威约束,沿用 SPEC-003 现有结构。

### 类型特异化约束(五条)

1. 结构特异非字符集 — 实验协议是结构层
2. 特异化需支撑 — 偏离率 K 由 N 除公式加 Jaccard 公式加收敛双指标均从 DES-005 v2 与 DES-010 引用
3. 不照抄通用 — 实验设计与 SPEC-003 路径二验收有结构差异
4. 不重复定义 — 不重写 DES-005 v2 已定义的偏离率公式
5. 最小化原则 — 只承载实验协议,不承载工程实现

### 哲学检索

任何决策前查哲学仓相关命题,特别是 convergence 层 P3.1 退化机制。

## 参考文件(最小必需集)

- `sih-engine/doc/design/DES-005-semantic-verification-calculus.md` — 偏离率方法学
- `sih-engine/doc/design/DES-010-single-key-multi-expert-experiment.md` — 实验设计
- `sih-engine/doc/spec/SPEC-003-crosscheck.md` — 当前参验规格

## 交付要求

SPEC-001 v2 完整实验协议,含接口签名(输入加输出加错误类型)、数据契约(语义判断材料结构加偏离率数值结构加 Jaccard 集合结构)、验收依据(波动系数阈值加 Jaccard 阈值加偏离率阈值,参数留空)、度量权威约束(LLM 是材料生成器)。doclint exit 0。最小化三问:动必要?是,SPEC v1 与 v2 范式不一致。动充分?是,实验协议完整。动最小?是,不发明新机制。
