# 金向量归一注记

净目标基形五Receipt冻结自 fixture 域根双跑实证（domain1 与 domain2，2026-09-13，lease 1.46.0，--at 2026-09-13T00:00:00+00:00 定值，identity 输入同源）：

- 跨根双跑差量仅两域：域根绝对路径（身份组件路径与 package_path）与开约签发 session_id。
- 其余全字段逐字节一致（路径归一后 cmp 实证，见 lease-lockcore-solo 批结果档 F-1）。
- T2 逐字节判据执行条件：比较前归一 session_id 与域根路径两域，此即 SPEC-024 同参形条款「session_id 归一项」修订候选的机械承载。
- close-receipt-idle.json 为空收约形（零锁收约跳归并删支）脏目标基形的净形对照位。
