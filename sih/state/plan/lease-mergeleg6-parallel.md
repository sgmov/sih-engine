# lease-mergeleg6-parallel 任务包（并联形）

## 形声明与令源

- 队形：并联形（parallel），主会立约取锁，并行簇四子代理后台各领一簇施工，主线验收结算。
- 令源：用户 2026-09-14 goal 续推令腿六；正典 SPEC-025。

## 目标（腿六十件加两裁断）

- 簇G：latex-helper（2539 行）。
- 簇H：wikirecall（1562）加 incubation（1110）。
- 簇I：confledger（553）加 locks（481）加 checker（348）加 basemgr（301）。
- 簇J：projsnap（263）加 acceptor（228）加 elicit（220）。
- 裁断两件不迁：proposition（DES/GOV/topics 纯命题数据区与批内一次性脚本，非生产工具）、askroute（判定包 intents-v0.json 数据加壳位归引擎 MCP 面，无独立 Python 工具）——判词随结果档入泊核销通道。

## 范围与要求（承两战先例）

- 每件：围堰只读对表，引擎新 bin（工地 src/bin/<tool>.rs），出参与退出码三值对表，测试件金向量三例起。
- 头 20 行内正典指针行（SDDG-2 硬约束）。
- 落差头注申报零粉饰；子代理只写工地源码与测试。
- 零新增 Cargo 依赖（A6）。

## 验收判据

- F1-F4 四簇 bin 加测试绿；F5 全族回归不破；F6 同参双跑抽验与级联。

## 明确不做

- 进程外插件桥实装让位收尾批；切换批另开。
- 围堰零改动。

## 改动文件清单

sih-engine/src/bin/{latextool,wikirecall,incubation,confledger,locks,checker,basemgr,projsnap,acceptor,elicit}.rs（新，命名避让见各簇单）、tests/mergeall_t6_*.rs（新）、本包、结果档与 materials、当日 trail。
