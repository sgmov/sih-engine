# orphanrule-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/orphanrule-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/orphanrule-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误（含判定与采样调用面节与 2026-09-07 新四条）

## 任务一句话

对命题 m-orphanrule-1（无主件处置与留痕规则三件：anchor.py 权限位件处置、活数据文件节奏 A/B 形选、CALL-LOG 强制随批检查位）跑完整得一裁——合同、九发采样、计分、核对、机器终签——三件各出裁定值，stable_clear 落据成规则，boundary 转人诊断。**本批只裁不改零实施**，处置执行归后继面。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package orphanrule-solo，repos 逐仓核对请求写入节且 materials 目录入列）→ 书简意图 → 工地施工 → 化格核阅检词 → 认证 → settle → 放锁收约 → 书单对表（checkcite 拼接扫描形）→ 对账对表。

## 四条命门（违反任何一条即批失败）

1. **命题即规范**：裁定依据只认命题照录材料（提案原文切片、协议句切片、机械事实复核命令）、归账批在档证据、哲学锚切片；P1 复核命令 `git -C sih-tools diff --summary attnanchor/anchor.py` 输出逐字节比对；P2 须实核 call-log rebuild 重建件在役证据——缺席即按判据裁 A 不可立，不硬凑。
2. **采样从工地、计分携正身**：facet 从工地跑；九发计分材料携 identity_hash，缺正身即拒。
3. **三态如实**：check 十二项、verify identical、sign 三态（refused 透传不掩）；boundary 即转人诊断呈报，**不重采样凑共识**。
4. **只裁不改**：两无主件本体、推导面、在役条款零触碰；本批零数值断言零实施；裁定值落档候执行面。

## 工程纪律

- 命题切片生成器 make_topic_orphanrule.py 在档，F-1 复核可重放；切片失配即按勘误条款材料修复重核
- checkcite 拼接扫描形；退出码直读禁管道掩码；主树零直写；守卫禁 plain commit
- 在飞会话 confpreempt-solo 持有零锁但在候清场——本批零触碰其面；撞锁排队候叫
- 在盘遗留无主件不豁免不代清，不新增无主写
- 收约后：链 verify、reconcile、泊界心跳、结果档 orphanrule-solo-results.md 落 event/plan（三件裁定值随档）、CALL-LOG 落笔、向用户完工报告

## 停批条件

命题切片与源文件大面积失配不可修复；P1 复核命令输出与命题照录不一致且不可解释；核对失败项无法以材料修正。停批即如实呈报现状，不硬闯不绕行。
