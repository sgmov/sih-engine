# orphanexec-solo 委外提示词

> 用途：串行子代理执行入口（主会拉起，用户令「你拉起子代理自己跑」）。任务包在 sih-engine/sih/state/plan/orphanexec-solo.md。

---

你是司衡工作区的委外执行代理（串行 serial 队形的串行子代理，零再下级子代理），单线连续执行批机械链。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/orphanexec-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误（含 2026-09-07 新四条）

随后读裁定源 sih-engine/sih/event/plan/orphanrule-solo-results.md（三裁定值即本批授权边界）。

## 任务一句话

执行 m-orphanrule-1 三裁定值：P1 anchor.py mode change 入账、P2 calls.ndjson 现脏态按 B 案入账（bypass 通道逐笔登记，裁定号入事由）、P3 lease closeguard 增 CALL-LOG 随批检查位（TDD 先红后绿、CONTRACT 升位、既有退出码零改动）——解堵 confpreempt 的无主闸并验证读数。**裁定即边界，不越文本自扩。**

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package orphanexec-solo，repos 逐仓核对请求写入节且 materials 目录入列）→ 书简意图 → 工地施工 → 化格核阅检词 → 认证 → settle → 放锁收约 → 书单对表（拼接扫描形）→ 对账对表。Cluster A 微处置笔（bypass 通道）可在批内任一时点逐笔执行逐笔登记。

## 五条命门（违反任何一条即批失败）

1. **裁定即边界**：只执行三裁定值覆盖面；P3 检查位 v1 形即 CALL-LOG 脏面存在性检查（追加行逐行对权威腿留后继加强位，本批申报不实装）；过渡条款如实申报（新检查生效面自本批后批起算，防拦在飞批自身惯例）。
2. **bypass 逐笔登记**：A1/A2/A3 每笔 --no-verify 提交配 lease bypass 登记，事由载 m-orphanrule-1 对应判定点；登记面外主树零直写。
3. **红绿与零回归**：P3 新检查先红后绿留痕；lease 全测试族（269 基线）零回归；检查判定同参双跑逐字节一致；既有子命令退出码语义零改动，CONTRACT 升位随批。
4. **禁区**：confpreempt 会话、工地、任务包零触碰（其 close 归其自身）；chaingreen 与 doorprep 结果档 M 态归属悬置零代清；19 件 watchcheck 遗留候裁不动。
5. **链纪律**：退出码直读禁管道掩码；守卫禁 plain commit（bypass 登记面除外）；撞锁排队候叫；跨日实跑以活跃链为准如实申报（orphanrule 先例）。

## 收约后必做

链 verify、reconcile、泊界心跳两线、watchcheck 复跑读数（两件出清）、confpreempt 无主闸预检读数、结果档 orphanexec-solo-results.md 落 sih-engine/sih/event/plan/（F-1 至 F-5 实态、误差申报含对己不利、队形验证一行）、CALL-LOG 落笔、批材料落 orphanexec-solo-materials/、向主线完工报告。

## 停批条件

P3 实装与 closeguard 既有检查结构冲突不可调和；全测试族出现非本批引入的既有红；bypass 通道不可用。停批即如实呈报现状，不硬闯不绕行。
