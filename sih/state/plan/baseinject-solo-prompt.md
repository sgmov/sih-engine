# baseinject-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/baseinject-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/baseinject-solo.md——本批全部判据与红线，其规格权威是 gateswitch 段三呈裁件 pk054-ab-gate-design.md，冲突时以设计档为准并如实呈报
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 候锁纪律（用户令：候其他任务结束后运行）

开工前查 locks.ndjson 现势与活跃会话；共享面（scribe/reports、identity/reports、CALL-LOG、BATCH-FACE、terms.json）被他会在持即排队候叫，不绕行不抢跑不预写。全链开始前先跑 watch 对表，无主清单只核对本批起草件两件（baseinject-solo.md 与 -prompt.md，已知在档候收编），其余遗留件不豁免不代清。

## 任务一句话

把席位标定的体温来源从「每天新跑 LLM 温度探针」改成「读档注入」——正身件＋标定账本在档读数＋pack 版本三样注入，零新鲜采样；按已裁 A/B 门四判据对跑 904 件冻结语料证明零判变；过门即拔闸退役探针两模式、移除 facet/.env 的 MINIMAX 键、账本转只读；pk-044 出泊。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package baseinject-solo，--allow 按任务包请求写入节）→ 书简意图 → 工地施工 → 化格核阅检词 → 认证 → 双仓 settle → 放锁收约 → 书单对表 → 对账对表。

## 四条命门（违反任何一条即批失败）

1. **零判变是唯一硬判据**：904/904 verdict 逐件三态全同；低于 100% 即停批，差异表逐件呈裁，不迁就不硬凑。注入值与冻结表数值逐位一致、两路漂移告警零、pk-035 哈希绑定不松动。
2. **零新鲜采样**：B 路全程禁 LLM 调用；账本 16 行历史零改写只读消费；注入物只限正身件、账本读数、pack 版本三样，塞散文进上下文即违反唯一桥梁裁定。
3. **退出码直读**：一切工具调用退出码直接读，禁管道掩码（genpark-solo 勘误在案）；命令面冻结，退出码语义零变化；温度数值管道保留只改来源，singleseat 单席位路径与采样合同 temperature 0.0 指令行零触碰。
4. **pk-044 出泊唯在 F-1 至 F-5 全过后**：exit 材料照录两笔裁定即独立性来源＝角度异质＋确定性透镜＋逃逸驱动增长（用户 2026-09-07 裁）、温度退役（用户 2026-09-07 裁），scribe park exit 落链。

## 工程纪律

- 先红后绿：注入路径新用例先证红再证绿，留痕结果档；既有测试零回归；金向量按族重放
- 引擎件合并主树后先重编再调用（旧二进制按旧码作答，BATCH-FACE 勘误）
- 主树零直写，待提交件经工地 settle 通道；守卫在位禁 plain git commit；.env 仅删 MINIMAX 一键
- 任务包与提示词两起草件随本批 allow 收编入 settle
- 收约后：链 verify、reconcile 双仓（退出码直读）、泊界心跳、结果档 baseinject-solo-results.md 落 event/plan、CALL-LOG 落笔、向用户完工报告

## 停批条件

F-1 一致率低于 100% 且无法以账本版本对齐修复；或拔闸后 tally assemble 基线断粮无法以注入同形件承接；或出现非本批引入的既有测试红；或撞锁排队超时。停批即如实呈报现状与差异表，不硬闯不绕行。
