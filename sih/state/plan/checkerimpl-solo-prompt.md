# checkerimpl-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/checkerimpl-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/checkerimpl-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

实装检查器（孵化登记件工作名）为空腹判定机：先把 sdd-v1 五包三十条判定面谓词机器化（封闭操作词汇表，义械双载体，契约补全走申报），再实装解释该词汇表的引擎（三值退出码、三态失败定位、双版本戳、同参双跑一致），以五包与样例五件为夹具先红后绿，validate.py 退役。工具落 sih-tools/checker/，契约权威在 sih-tools/incubation/regula-line-tools-2026-09-06.md。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package checkerimpl-solo，双仓工地 engine 与 tools，--allow 按任务包请求写入节）→ 书简意图 → 工地施工（批输入件拷入工地、CONTRACT 加包机器化、测试先红、引擎至绿、冻结向量与退役）→ 化格核阅检词 → 登记件验收判据对表 → facet 三步 → 得一三步（check／verify／sign）→ 认证 → 双仓 settle → 放锁收约 → 书单对表 → 对账对表 → 结果档与完工报告。

## 四条命门（违反任何一条即批失败）

1. **空腹是构成性承诺**：引擎只实现词汇表通用操作，grep 测试证明 src 零 SDD 特异字面零规则 ID 字面；三十条里有规则落不进 v0 词汇表即停批上报，不私扩词汇不硬编码。
2. **先红后绿是本线自家纪律**：测试先行、空实现跑红、红证归档，然后实装至绿——本批是文规线第一次吃自己的药，红证不齐即 F-3 红线。
3. **退出码直读**：一切工具调用退出码直接读，禁管道掩码（genpark-solo 勘误在案）。
4. **零越权**：零租约源码写入（租约线在修）、零引擎源码写入、零 sih-math 写入；契约补全走结果档申报不改登记件；在泊件与在盘遗留零触碰；人节点零代行；守卫禁 plain commit。

## 工程纪律

- 工程形照 formatter 先例（uv 加 src 布局）；报告 JSON 双版本戳；无网络无隐式状态
- 围堰基线向量（绿五件加红样例的冻结期望报告）落包目录，manifest 申报迁移点即基线向量管理工具建成后迁移
- 受检文档避免懒波词面全称，用检查器简称（检词 lazy 命中规避）
- 主树零直写（开工后第一动作把任务包与提示词从主树拷入工地，此后零主树写）
- 收约后：链 verify、reconcile 双仓（退出码直读）、泊界心跳、结果档 checkerimpl-solo-results.md 落 event/plan 含契约补全申报节与验收判据对表节、CALL-LOG 落笔、向用户完工报告（含每步退出码、双仓提交号、终签哈希、越线与误差申报）

## 停批条件

得一裁不收敛；词汇表覆盖不足；与登记件契约冲突无法以 as-built 披露化解；需新造中文词；撞锁排队超时或链真分叉。停批即如实呈报现状，不硬闯不绕行。
