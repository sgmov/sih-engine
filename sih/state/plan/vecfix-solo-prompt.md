# vecfix-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/vecfix-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/vecfix-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

修复基线向量管理工具批的两处收约后缺陷：其一，七支冻结向量（checker-golden 六支加 self/cli-help）的命令串嵌已拆除的工地绝对路径，按 `{ROOT}` 归一形重冻——重冻前后 expected_sha256 必须逐支一致，路径归一是寻径层不是语义层，哈希漂移即停批；其二，freeze 侧加命令归一规则、数据面加 grep 守卫（vectors 零 worktrees 字面）、检查器测试版本钉改从 manifest 动态读。收口判据唯一：TDD 验收工具判定包端到端复绿，批内与收约后主树双读数。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 缺陷复现三项留痕（任务包 §一复现形）→ 租约开工（--package vecfix-solo，双仓工地，--allow 按请求写入节）→ 书简意图 → 工地施工（批输入件拷入工地、守卫测试先红、七支归一重冻哈希对表、freeze 归一规则、版本钉动态对表、批内全绿）→ 化格核阅检词 → facet 三步 → 得一三步（check／verify／sign）→ 认证 → 双仓 settle（声明件先提交后 close，差集闸在位）→ 放锁收约 → 书单对表 → 对账对表 → **主树复跑判定包退绿回填补笔节** → 结果档与完工报告。

## 四条命门（违反任何一条即批失败）

1. **期望字节零变**：任一支重冻后 expected_sha256 与重冻前不一致即停批上报——那是真语义差异不是路径归一，禁止重冻掩盖。`.bin` 期望件与 kind 标注零触碰。
2. **先红后绿**：守卫测试与版本钉修复先行在缺陷态跑红、红证归档，然后修到绿——红证不齐即停。
3. **退出码直读**：一切工具调用退出码直接读，禁管道掩码（genpark-solo 勘误在案）。
4. **最小改动面零越权**：只动基线向量管理工具 src 与 tests 与 vectors、检查器 tests、批件面；零租约零引擎零 sih-math 源码写入；在泊件与在盘遗留零触碰；人节点零代行；守卫禁 plain commit。

## 工程纪律

- 归一范围以工作区根前缀为界，不得误伤系统路径（解释器位等），测试覆盖此边界
- 受检文档用基线向量管理工具与 TDD 验收工具指称，规避懒波词面全称命中
- 主树零直写（开工后第一动作把任务包与提示词从主树拷入工地，此后零主树写）
- 收约后：链 verify、reconcile 双仓（退出码直读）、泊界心跳、判定包主树复跑退绿读数回填、结果档 vecfix-solo-results.md 落 event/plan 含缺陷复现红态节与主树复跑补笔节、CALL-LOG 落笔、向用户完工报告（含每步退出码、双仓提交号、终签哈希、越线与误差申报）

## 停批条件

得一裁不收敛；任一支哈希漂移；归一规则无法以工作区根为界实现；需新造中文词；撞锁排队超时或链真分叉。停批即如实呈报现状，不硬闯不绕行。
