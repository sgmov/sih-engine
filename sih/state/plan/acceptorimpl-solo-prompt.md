# acceptorimpl-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/acceptorimpl-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/acceptorimpl-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

实装 TDD 验收工具（孵化登记件工作名）为空腹判定包执行机：四查即数据（双跑一致、基线冻结逐字节比对、红证哈希加当前绿、场景覆盖差集），引擎解释判定包逐查跑逐查记聚合退出码；首例判定包吃 sdd-v1 与 checker 线自己的 golden 与红证（自反狗粮闭环）。工具落 sih-tools/acceptor/，契约权威在孵化登记件验收工具节，v0 范围外（三态归因全量机制属基线向量管理工具后继）双申报不伪装已全。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package acceptorimpl-solo，双仓工地 engine 与 tools，--allow 按任务包请求写入节）→ 书简意图 → 工地施工（批输入件拷入工地、CONTRACT 加首例判定包、测试先红、引擎至绿、四查三态实测）→ 化格核阅检词 → 登记件验收判据对表 → facet 三步 → 得一三步（check／verify／sign）→ 认证 → 双仓 settle → 放锁收约 → 书单对表 → 对账对表 → 结果档与完工报告。

## 四条命门（违反任何一条即批失败）

1. **空腹是构成性承诺**：命令与项目知识只进判定包数据，grep 测试证明引擎 src 零项目特异字面零命令字面零规则编号字面；词汇表外新操作即停批上报不硬编码。
2. **先红后绿与 v0 诚实**：测试先行红证归档后实装至绿；三态归因全量机制是后继不是本批，CONTRACT 与结果档双申报，不伪装已全。
3. **退出码直读**：一切工具调用退出码直接读，禁管道掩码（genpark-solo 勘误在案）。
4. **零越权**：零租约零引擎零 sih-math 源码写入；checkerimpl 红证材料只读引用零触碰；契约出入走 as-built 披露不改登记件；在泊件与在盘遗留零触碰；人节点零代行；守卫禁 plain commit。

## 工程纪律

- 工程形照 checker 先例（uv 加 src 布局加 CONTRACT 加围堰 golden）；报告载声明范围字段即甲乙红线（基线类查只主张没变不主张对）
- 受检文档用「TDD 验收工具」指称，规避懒波词面全称命中
- 主树零直写（开工后第一动作把任务包与提示词从主树拷入工地，此后零主树写）
- 收约后：链 verify、reconcile 双仓（退出码直读）、泊界心跳、结果档 acceptorimpl-solo-results.md 落 event/plan 含 v0 范围申报节与验收判据对表节、CALL-LOG 落笔、向用户完工报告（含每步退出码、双仓提交号、终签哈希、越线与误差申报）

## 停批条件

得一裁不收敛；词汇表覆盖不足或判定包 schema 与契约冲突无法以 as-built 披露化解；判据程序寻径不通；需新造中文词；撞锁排队超时或链真分叉。停批即如实呈报现状，不硬闯不绕行。
