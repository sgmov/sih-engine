# confpreempt-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/confpreempt-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/confpreempt-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误（含 2026-09-07 新四条）

## 任务一句话

给置信度经济装上抢占环：规则面三件（触发条件、价格机制形、报销证据标准）过得一裁（m-confpreempt-1），公理钉死的五步环机械实装（付不退→裁→放锁落链→被抢方处置→余款充公铸给主动上链者），lease 与 confledger 双侧 additive 对接——**规则只从裁出、数值只从三通道出、在役面零改动**。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package confpreempt-solo，repos 逐仓核对请求写入节且 materials 目录入列）→ 书简意图 → 工地施工（命题与一裁在前，实装随后）→ 化格核阅检词 → 认证 → settle → 放锁收约 → 书单对表（checkcite 拼接扫描形）→ 对账对表。

## 五条命门（违反任何一条即批失败）

1. **规则只从一裁出**：命题三判定点的裁定依据只认 A4 原文、v3 推导档、在役机制事实（takeover 探针与心跳与 wait-turn 先例照录程序切片禁手打）；boundary 如实转呈，不重采样凑共识。
2. **数值只从三通道出**：价格机制形可裁，价格数值必须走归一／推导／标定通道——需数值而无通道即挂槽位态，代码零硬编码，constants 面零新数值。
3. **公理环机械零自由度**：付不退（无退款算子）、欠账拒抢占、余额不足整笔拒（fail-closed 零部分支付）、无罪回滚零罚金——四守卫照 A4 与 confledger v0 既有算子落，不得改支付算子语义。
4. **在役面零改动**：lease 与 confledger 既有子命令退出码与判据零触碰，新增面 additive；报销走 m_repair 槽位休眠记 IOU 不兑付。
5. **红绿与验收**：四守卫与五步环夹具全走通，先红后绿留痕，acceptor 判定包词汇表内操作承载，不私扩。

## 工程纪律

- 采样从工地 facet 跑、计分携正身件；命题引文程序切片逐字节（F-1 红可修复即修复重核的勘误条款在案）
- 主树零直写；守卫禁 plain commit；退出码直读；在飞批撞锁排队候叫
- 在盘遗留无主件不豁免不代清，不新增无主写
- 收约后：链 verify、reconcile、泊界心跳、结果档 confpreempt-solo-results.md 落 event/plan（裁定值随档）、CALL-LOG 落笔、向用户完工报告

## 停批条件

裁定值与在役 takeover 语义出现不可调和冲突且呈裁无解；一裁 boundary 三件全转；支付算子对接不通且无 additive 路径。停批即如实呈报现状，不硬闯不绕行。
