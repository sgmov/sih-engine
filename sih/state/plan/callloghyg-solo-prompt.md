# callloghyg-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/callloghyg-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/callloghyg-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

把 14 件主树脏态 CALL-LOG 逐行归属披露后按补笔先例归账，清 cert_missing 与陈旧会话旧账，出两件规则面提案——**他批在飞件零触碰、未裁件零代清、规则只提案不实施**，完工即解除 confpreempt 的无主阻塞。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package callloghyg-solo，repos 逐仓核对请求写入节且 materials 目录入列）→ 书简意图 → 工地施工 → 化格核阅检词 → 认证 → settle → 放锁收约 → 书单对表（拼接扫描形）→ 对账对表。

## 四条命门（违反任何一条即批失败）

1. **归属零错配**：每行 CALL-LOG 的批名与链上记录（结果档、CALL-LOG 既有行、trail 事件）对得上才入账；对不上即悬置申报，不硬归不猜测。
2. **他批零触碰**：attnanchor／calllog 等在飞会话的生产物一件不动；19 件 watchcheck 遗留维持候裁态。
3. **bypass 逐笔登记**：归账走补笔通道每笔落台账，主树其余零直写。
4. **规则只提案**：活数据文件节奏与 CALL-LOG 强制随批两提案落结果档候裁，本批零实施。

## 工程纪律

- 开工前查活跃会话：attnanchor 与 calllog 等未收约即顺延窗口（排队候叫先例），不与在飞批抢主树面
- 退出码直读；守卫禁 plain commit（bypass 面按登记通道走）
- 收约后：链 verify、reconcile、watchcheck 复跑读数、confpreempt 无主闸预检读数、结果档 callloghyg-solo-results.md 落 event/plan、CALL-LOG 落笔、向用户完工报告

## 停批条件

活跃会话长期不收致窗口关闭；行归属大面积对不上链记录（超半数悬置）；bypass 通道不可用。停批即如实呈报现状，不硬闯不绕行。
