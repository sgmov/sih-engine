# basemgrimpl-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/basemgrimpl-solo.md，本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/basemgrimpl-solo.md——本批全部判据与红线
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

实装基线向量管理工具（孵化登记件工作名，金向量双种共享家位）：freeze、replay、refreeze 三子命令（登记件冻结的 CLI 形不得私改），向量件载冻结四元组加环境指纹加版本三元积，重冻分种即基线种漂移归因前置、规约种先红前置；五子模块细目按登记件授权由本批设计申报入 CONTRACT。落成后做双机迁移——检查器 golden 六件与 TDD 验收工具 frozen 件迁入新家，迁后重放证零漂移，检查器包族 D-4 锚位回填翻真，工具自身金向量自托管。工具落 sih-tools/basemgr/。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → 租约开工（--package basemgrimpl-solo，双仓工地 engine 与 tools，--allow 按任务包请求写入节）→ 书简意图 → 工地施工（批输入件拷入工地、CONTRACT 五子模块设计申报、数据形成文、测试先红、引擎至绿、三查实测、双机迁移与重放证、锚位回填、自托管）→ 化格核阅检词 → 登记件验收判据三查对表 → facet 三步 → 得一三步（check／verify／sign）→ 认证 → 双仓 settle（声明件先提交后 close，差集闸在位）→ 放锁收约 → 书单对表 → 对账对表 → 结果档与完工报告。

## 四条命门（违反任何一条即批失败）

1. **契约形不得私改**：CLI 三子命令、四元组、指纹位、重冻分种、甲乙红线、逐字节门优先，全是登记件已裁边界；五子模块设计申报不越界，缝大即停批上报。
2. **v0 诚实与先红后绿**：覆盖机（探针、分块矩阵、覆盖率）是后继不是本批，CONTRACT 与结果档双申报不伪装已全；测试先行红证归档后实装至绿。
3. **退出码直读**：一切工具调用退出码直接读，禁管道掩码（genpark-solo 勘误在案）。
4. **零越权**：零租约零引擎零 sih-math 源码写入；checkerimpl 红证原档只读零触碰；迁移动检查器 manifest 与验收判定包两件数据须逐件版本进位申报；在泊件与在盘遗留零触碰；人节点零代行；守卫禁 plain commit。

## 工程纪律

- 工程形照 checker 与 acceptor 先例（uv 加 src 布局加 CONTRACT 加测试）；操作词汇封闭，词汇表外即停批；grep 空腹证明（src 零项目特异字面零命令字面零规则编号字面）
- 环境指纹白名单以最小集起步并申报，不完备性承 T11 条款声明；无申报影响集的漂移即未分类，refuse 重冻不硬闯
- 受检文档用「基线向量管理工具」指称，规避懒波词面全称命中
- 主树零直写（开工后第一动作把任务包与提示词从主树拷入工地，此后零主树写）
- 收约后：链 verify、reconcile 双仓（退出码直读）、泊界心跳、结果档 basemgrimpl-solo-results.md 落 event/plan 含五子模块设计申报节与 v0 范围申报节与验收判据对表节、CALL-LOG 落笔、向用户完工报告（含每步退出码、双仓提交号、终签哈希、越线与误差申报）

## 停批条件

得一裁不收敛；gvec-v2 修订稿与 v0 实装的缝无法以范围申报承载；迁移重放出现非零漂移（真迁移损失即停批上报不重冻掩盖）；需新造中文词；撞锁排队超时或链真分叉。停批即如实呈报现状，不硬闯不绕行。
