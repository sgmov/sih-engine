# doorsimpl-solo 委外提示词

> 用途：由用户转发给委外执行代理。任务包在 sih-engine/sih/state/plan/doorsimpl-solo.md（**SDD 形首个强制食客**，已过五包出生彩排），本提示词是执行入口。

---

你是司衡工作区的委外执行代理，单线 solo 队形、零子代理。工作区根：/Users/moc/workspaces/SiHankor。

## 开工三读

1. 读 AGENTS.md（工作区根）——治理宪法与工程基线
2. 读任务包 sih-engine/sih/state/plan/doorsimpl-solo.md——SDD 形，六需求八场景，全部判据与红线在内
3. 读 sih-tools/BATCH-FACE.md——批机械链逐命令 verbatim 与坑位勘误

## 任务一句话

把两道门挂上租约闸：开约挂 SDD 门（检查器按 flow-v1 流程包验任务包件族，退出码 1 即拒开工）、收约挂 TDD 门（TDD 验收工具按流程包跑查族，聚合非零即拒收约），门事件三态上链，门实现零硬编码（grep 证明），既有命令面零破坏，未声明流程包的类灰度跳过记链。**本批自身收约要走自己刚立的 TDD 门——立门的批第一个过门收约。**

## 开工前置（硬条件）

租约线收官宣告在案（attnanchor 与 confpreempt 两批收约、链面回退缺陷处置在案）。未收官即排队候位零写入，如实呈报。

## 批机械链全序（照 BATCH-FACE 逐命令跑，不跳步）

三问双门 → 叩问 → 正身 → SDD 门彩排（任务包五节抽取验形过五包）→ 租约开工（--package doorsimpl-solo，双仓工地，--allow 按任务包十二节逐路径分行）→ 书简意图 → 工地施工（挂点测绘、门测试先红、门模块实装至绿、灰度与回归）→ 化格核阅检词 → facet 三步 → 得一三步 → 认证 → 双仓 settle（声明件先提交后 close，差集闸在位）→ **本批 close 走自己实装的 TDD 门** → 放锁收约 → 书单对表 → 对账对表 → 主树复跑判定包退绿 → 结果档与完工报告。

## 四条命门（违反任何一条即批失败）

1. **门规全数据零硬编码**：件族与查族映射只从 flow-v1 读，租约门模块源码零包路径零任务类字面（grep 测试证明）；流程包寻径失败即门拒报缺件不硬闯。
2. **既有命令面零破坏**：租约既有子命令、旗标、退出码语义零变化，门是加法；既有测试全族零回归；门拒不留半开状态。
3. **先红后绿**：门测试先行（坏包拒、漂移拒先红证归档），后全绿（好包过、绿判定包过、灰度跳过、回归全族）。
4. **退出码直读零越权**：一切工具调用退出码直接读，禁管道掩码；三机零改（检查器、TDD 验收工具、基线向量管理工具）；在泊件与在盘遗留零触碰；人节点零代行；守卫禁 plain commit。

## 工程纪律

- 挂点实装前先对 1.33.0 的 open 与 close 校验链做挂点测绘（SDD 门在 allow 面解析后工地建立前、TDD 门在差集闸后），缝大即停批上报
- 门事件三态复用书简 append 通道，读数载判定摘要与包版本
- 本任务包是 SDD 形首个食客：单文件五节，节抽取验形不改内容（出生彩排已过，你的 S-001 步复跑正式读数入批材料）
- 受检文档用检查器、TDD 验收工具、基线向量管理工具指称，规避懒波词面全称命中
- 主树零直写（开工后第一动作把任务包与提示词从主树拷入工地，此后零主树写）
- 收约后：链 verify、reconcile 双仓、泊界心跳、主树判定包复跑退绿读数回填、结果档 doorsimpl-solo-results.md 落 event/plan 含 SDD 门彩排读数节、CALL-LOG 落笔、向用户完工报告（含每步退出码、双仓提交号、终签哈希、越线与误差申报）

## 停批条件

得一裁不收敛；挂点与 1.33.0 校验链的缝无法以加法承载；租约线未收官且排队超时；需新造中文词；链真分叉。停批即如实呈报现状，不硬闯不绕行。
