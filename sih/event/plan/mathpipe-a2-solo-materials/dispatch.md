# mathpipe-a2-solo 执行指令

你在 SiHankor 工作区 /Users/moc/workspaces/SiHankor 执行治理批 mathpipe-a2-solo。先读 /Users/moc/workspaces/SiHankor/AGENTS.md 与任务包 /Users/moc/workspaces/SiHankor/sih-engine/sih/state/plan/mathpipe-a2-solo.md 与本指令，调用面照 /Users/moc/workspaces/SiHankor/sih-tools/BATCH-FACE.md。派工序：本批为三批之末（ledgrev-solo 与 entryunique-solo 之后），同一时点仅一批在途，撞当日链锁即等待不绕行。

## 会话启动（首动作，缺一不可）

1. 例行读数：gauge record 全参形，--session 即 ask3 记录 session_id。
2. 泊界心跳：attractor route 两线材料目录，告警与否如实记。

## 人节点前置（开工前必取）

向用户取显著性水平 α 取值裁定（推荐 0.05 即承源码 ALPHA 与通行水平；取值由人裁，正确性由数学检验）。未取即停，不默认开工。用户原话留档 materials 与 inputlog。

## 批内容要点

1. 载体定位：全读 sih-math/llm-friendly-build/mapping.md（消费面验收线即全读），定二项检验与显著性检验载体，推导档承载检验统计量与零假设与备择与 α 语义，落 sih-math/docs/。零命中即停批走 M-3 上报，不代建新载体。
2. 判据切换：r3a_gate_v2.py 判定路径从 BOUNDARY_RATE_THRESHOLD 占比阈值切换为检验判据，接线 facet_stats_inf.py 函数族，α 显式给参不读钟。
3. 金向量：切换后 gate 输出冻结与复算逐字节一致。
4. 判变申报：切换若改变既有裁决结果逐件列判变清单待人确认，零判变如实记零，不静默改判。
5. 范围纪律：仅判定路径两常数位（判据位与 α 位）；probes 分析件阈值与围堰外工具零改动。

## 机械链（照 BATCH-FACE 全序）

ask3 记录（session_id 形如 sess-zcode-<实日无横线>-mathpipea2；三锚引文程序切片禁手打，锚位即 07-on-assay.md:61 判据映照非投射、06-on-canon.md:185 判据由松到紧即检验判据替经验阈值、08-on-settle.md:108 判变如实申报）→ 双门 → elicit check 逐词 --words（词单如实跑，如 检验判据 与 推导档 与 判变清单）→ digest（契约逐词叩问处置标记）→ 正身 → lease open --package mathpipe-a2-solo --repo sih-engine --repo sih-tools --repo sih-math --root 绝对（allow 一次列全即冻结）→ 取锁 → meter 包裹 scribe intent → 工地三仓施工 → 管线三步（推导档与变更件；math docs 域外 exit-2 如实记）→ 认证逐件 append → 三仓 settle → 放锁 → close（备份让位归并对表法）→ reconcile 三仓零新增 → 链 verify → 调用册留痕 → 结果档。

## 结果档（起草前必跑切面检索）

retriever recall --event certification_completed --since <开工日> --until <止日> --at <实日> --out materials/recall-results.json，链上事实以切面为底稿。F 表写全，判变清单逐件在档。

## 红线

停批条件即载体零命中或判变待人确认期间，不硬行；α 未裁不开工；probes 分析件零改动；金向量冻结零漂移；守卫在位严禁 plain git commit 直提；上链前等绿；findings 亲读禁管道掩退出码；撞锁即停批报告。

## 完工报告（最终回复直接输出）

意图哈希、α 裁定留档位、载体定位结论（载体 ID 与推导档路径或停批如实）、判据切换与金向量读数、判变清单（逐件或零）、认证清单、三仓 commit 号、链 verify 对表、reconcile 读数、F 表、越线与误差申报。
