# mathquote-solo 执行指令（冲突模式）

你在 SiHankor 工作区 /Users/moc/workspaces/SiHankor 执行治理批 mathquote-solo。先读 /Users/moc/workspaces/SiHankor/AGENTS.md 与任务包 /Users/moc/workspaces/SiHankor/sih-engine/sih/state/plan/mathquote-solo.md 与本指令，调用面照 /Users/moc/workspaces/SiHankor/sih-tools/BATCH-FACE.md。

## 冲突模式声明（用户令，承 pk-045）

本批与他批（guardrail-solo 与 mathrefmt-solo）故意并发。撞他会话锁即等待有限重试（上限十次逐次计数），每次冲突记一条入结果档冲突样本节。认证一律先落主树活链，链文件 settle 前一次性拷工地，严禁工地链副本追加。收约撞主树走备份让位归并对表法，非 identical 即停批上报。本批只动四子仓条目，与 mathrefmt 的 calculus 三前缀零重叠，冲突预期在共享台账面。

## 会话启动（首动作）

例行读数 gauge record 全参形；泊界心跳两线材料目录。

## 批内容

四子仓逐字引文补强：枚举脚本扫 topology 与 probability 与 order 与 algebra 四子仓已建条目哲学桥接缺「原文「」标记的清单；逐条按命题对照的 PRO 编号回 sih-philosophy/emanation/proodos/ 原文定位引文，**程序切片逐字节子串禁手打**（python 对原文做子串断言后再入文），落形即原文「引文」见 文件:行号，先例即 order/entries/ORD-015；命题对照保留不替换；复验脚本对全部新增引文做原文子串断言全绿可重跑；calculus 零触碰归后续波。

## 机械链

照 BATCH-FACE 全序：ask3（三锚引文程序切片即 07-on-assay.md:61 与 06-on-canon.md:185 与 08-on-settle.md:108，session_id 形如 sess-zcode-<实日无横线>-mathquote）→ 双门 → elicit 加 digest → 正身 → lease open --package mathquote-solo --repo sih-engine --repo sih-tools --repo sih-math --root 绝对（allow 一次列全即冻结）→ 取锁（撞锁重试计数）→ meter 包裹 scribe intent → 工地三仓逐条补写 → 复验脚本全绿 → 逐条三步（化格加 des-001-mathe 域内零违规加检词）→ 认证逐件 append 主链 → 三仓 settle → 放锁 → close（备份让位对表法）→ reconcile → 链 verify → 调用册追加 → 结果档（含冲突样本节，retriever recall --event 切面为底稿）。

## 红线

哲学仓只读；引文禁手打必须程序切片加子串断言；calculus 零触碰；上链前等绿 findings 亲读。

## 完工报告（最终回复直接输出）

意图哈希、枚举计数与本波补写件数、复验脚本读数、三步读数、冲突样本节逐条、认证清单、三仓 commit 号、链 verify 对表、reconcile 读数、F 表、越线与误差申报。
