# mathrefmt-solo 执行指令（冲突模式）

你在 SiHankor 工作区 /Users/moc/workspaces/SiHankor 执行治理批 mathrefmt-solo。先读 /Users/moc/workspaces/SiHankor/AGENTS.md 与任务包 /Users/moc/workspaces/SiHankor/sih-engine/sih/state/plan/mathrefmt-solo.md 与本指令，调用面照 /Users/moc/workspaces/SiHankor/sih-tools/BATCH-FACE.md。

## 冲突模式声明（用户令，承 pk-045）

本批与他批（guardrail-solo 与 mathquote-solo）故意并发。撞他会话锁即等待有限重试（上限十次逐次计数），每次冲突记一条入结果档冲突样本节。认证一律先落主树活链，链文件 settle 前一次性拷工地，严禁工地链副本追加。收约撞主树走备份让位归并对表法，非 identical 即停批上报。本批与 mathquote-solo 写入面按前缀与子仓分区零重叠即 mathquote 动四子仓你动 calculus 三前缀，共享面即链与台账与调用册，冲突预期在锁不在内容。

## 会话启动（首动作）

例行读数 gauge record 全参形；泊界心跳两线材料目录。

## 批内容

calculus 概览优先旧格式归一：枚举脚本扫 calculus/llm-friendly-build/entries 首二级节为「概览」的全量清单落 materials；本波做 LIM 与 DIFF 与 INT 三前缀全量，余前缀切片边界申报后续波；逐条把概览节归入定义节即概览首段为定义引入、余段按语义落定义或公理条件，段落去向逐段申报零弃段；状态行与命题对照与关系节零改动；INDEX 计数与 VERSION 基线零触碰。

## 机械链

照 BATCH-FACE 全序：ask3（三锚引文程序切片即 07-on-assay.md:61 与 06-on-canon.md:185 与 08-on-settle.md:108，session_id 形如 sess-zcode-<实日无横线>-mathrefmt）→ 双门 → elicit 加 digest → 正身 → lease open --package mathrefmt-solo --repo sih-engine --repo sih-tools --repo sih-math --root 绝对（allow 一次列全即冻结）→ 取锁（撞锁重试计数）→ meter 包裹 scribe intent → 工地三仓逐条改写 → 逐条三步（化格加 des-001-mathe 域内零违规加检词）→ 认证逐件 append 主链 → 三仓 settle → 放锁 → close（备份让位对表法）→ reconcile → 链 verify → 调用册追加 → 结果档（含冲突样本节，retriever recall --event 切面为底稿）。

## 红线

缺引文条目不属本波不混做；INDEX 与 VERSION 零触碰；S005 域内零违规；上链前等绿 findings 亲读。

## 完工报告（最终回复直接输出）

意图哈希、枚举计数与本波件数、段落去向申报表摘要、三步读数、冲突样本节逐条、认证清单、三仓 commit 号、链 verify 对表、reconcile 读数、F 表、越线与误差申报。
