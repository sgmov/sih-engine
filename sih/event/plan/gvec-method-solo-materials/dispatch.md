# gvec-method-solo 执行指令

你在 SiHankor 工作区 /Users/moc/workspaces/SiHankor 执行治理批 gvec-method-solo。先读任务包 /Users/moc/workspaces/SiHankor/sih-engine/sih/state/plan/gvec-method-solo.md 与本指令，调用面照 /Users/moc/workspaces/SiHankor/sih-tools/BATCH-FACE.md，facet 全流程照 /Users/moc/workspaces/SiHankor/sih-engine/sih/event/plan/autoflow2-solo-materials/dispatch.md 件二先例。

政策行（用户裁定 2026-09-03 原话照录）：「等我裁的东西首先过得一，得一有问题的异常才让我看」——件一纯机械机械链全绿即闭环；件二得一裁 stable_clear 即签核执行、boundary 或不过即进泊界，均不扰人节点，得一异常才如实上报。

## 件一：全量金向量用法审计（纯机械）

1. 枚举脚本扫三仓金向量位点：engine cargo 测试 golden 系（src/scrutinator/tests.rs 等）、sih-engine 内 scrutinator fixtures/golden、sih-tools 各工具 vectors 目录、sih-engine/sih/event/plan/*-results.md 的 F 表金向量断言行。
2. 逐断言分类：甲类变更检测（重放一致/行为零变更/漂移报警，声明范围内健全）与乙类隐含正确性主张（由重放一致推出正确或无误，越权）。分类只读不改，乙类清单即漏洞清单。
3. 读数与清单落 materials 可 grep 复算。

## 件二：金向量方法论命题先裁后行（得一裁）

1. 命题起草照任务包三节原文：gid gvec-method-guard-1，SPEC-017 四条款加 ORD-007 扩展挂靠。对己不利声明双倍显式入 topic 即元层自指（facet 自身用金向量）加利益披露（后续每批 F 表负担加重）。
2. 全流程引擎件出裁：emit-contract → 九发独立重采（禁复用旧响应）→ score → check；seat 基线对表当日最新标定件，漂移即重标定承先例。
3. 三态分流：stable_clear → sign 终签入链 → 执行落 doc/spec/SPEC-017-golden-vector-lifecycle.md（四条款成文承任务包原文）→ SPEC-017 走管线域内零违规；数学仓 ORD-007 扩展只出草案挂 results，扩条目批另开不越权。boundary 或不过 → 不签不硬来 → 泊界登记（entry_id 取当日链未用号，停泊事件经书简 park 入链，PARKING-v1.md 名册更新）。

## 机械链（照 BATCH-FACE 全序）

ask3 记录（三锚引文程序切片禁手打：07-on-assay 镜即纯粹映照、06-on-canon 由松到紧、08-on-settle 应而不藏）→ 双门 → 叩问 elicit check --packs sih-tools/nomenclator/packs/core --words 锚集 --words 漂移归因 --words 金向量方法论 digest passed → 正身 → lease open --package gvec-method-solo --repo 三仓绝对路径（一切锁操作显式 --session）→ 取锁（请求写入节逐路径）→ meter 包裹引擎 scribe intent 上链（--trail 主树绝对路径）→ 工地施工 → 管线三步（SPEC-017 域内笔在核前判在书简前；findings 亲读）→ 认证逐件 meter 包裹 append 主树活链 → 链 settle 前一次性拷工地 → settle --seq N --cert 前八位 → 放锁 → close（备份让位归并对表法）→ reconcile → 当日链 verify。

## 冲突模式（承 pk-045）

撞锁有限重试上限十次逐次计数；冲突样本节必载（零撞锁如实记零）；收约前先放锁后拆工地。

## 红线

审计只分类禁改任何金向量与测试；命题不过不硬签；主树零直写；守卫在位严禁 plain git commit 直提；findings 亲读；禁管道掩退出码；identity/reports 与既有存量 untracked 零收编。

## 完工报告（最终回复直接输出）

意图哈希、件一枚举计数与甲乙分类读数与乙类漏洞清单、件二裁决结论（gid、gate、disposition、逐发分布）与三态实态（SPEC-017 落位或泊界事件哈希）、认证清单、三仓 commit 号、链 verify 前后对表、reconcile 读数、F 表、冲突样本节、越线与误差申报。
