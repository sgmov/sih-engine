# lease-mergeleg23-parallel 批结果档

## 结算要点

- 批名 lease-mergeleg23-parallel（并联形；stem 甲表认领 mergeleg23:new，概念锚 zh 融回腿二三，code 形显式申报无承，终裁登记候人节点）
- 会话 de802d40a3f3fae4 双仓，五锁，意图笔在链；正典 SPEC-025 腿序
- 令源用户 2026-09-14 goal 续推令；并联三簇子代理并行施工，主线验收结算

## 交付九件（围堰约 5000 行 → 引擎 Rust）

- 簇D：parser（2958 行 Rust，四子命令全量移植，词法与空腹 PEG 与条目投影与 lint 与 vectors；自足回溯 VM 移植 Python re 特有构造承零依赖纪律；json 加 markdown 加 rust 三真包 parse 树与 entries 与 lint 与 vectors 对 Python 冻结金向量逐字节一致）加 locator（2864 行，build 加 query 加 stale，五载体索引，code 载体内嵌句读紧凑副本；vectors 语料条目与五类查询对冻结期望逐字节一致，双跑一致）。
- 簇E：selector（pack 驱动三路路由，谓词链十一件，告警两面）加 cascade（build 加 check 加 orphans，四态上游洁净）加 identity（正身 v3 十二件加盐 SHA-256，attest 与 verify 双模式，盐哈希与核哈希经 Python hashlib 独立复算逐字节一致）。
- 簇F：tally（R1-R9 核对加 DES-011 三态四值处置，规范形经真 Python json.dumps 交叉验证）加 meter（包裹执行加计数册加 crosscheck 链上对表漏计检出）加 watchcheck（porcelain 解析加锁面镜像加 16 面豁免差集谓词，真 git 金向量）加 calllogtool（append 两腿齐落加 render 加 reconcile；sqlite 腿承 A6 申报未实装；选名避让首战批引擎 lease 内 calllog 模块）。

## 验收对表

- 主验收复跑：新测 27 全绿（九件各三例），lease 全族 11 与首战批六套件回归全 ok，工地全量编译零错误。
- 同参双跑：parser 三真包逐字节（施工中留痕）、locator vectors 逐字节、identity 哈希 hashlib 独立复算、tally 真 json 交叉验证、watchcheck 真 git 构造——五件带逐字节级对表证据。
- SDDG-2：九 bin 正典指针均头 20 行内（簇D第 2 行、簇E第 2 行、簇F awk 验证）。
- 施工注记：tempdir 跨进程可见性迟滞窗口（run_stable 稀疏重试承接头注申报）与一例测试预期对表 Python 修正（工具行为无误）如实登记。

## 队形验证

并联三簇一次成立：三子代理独立施工零改派，跨簇编译互挡经各簇如实申报并自行消化，主线验收一次过。

## 偏差

- 范围绕行偏差：腿四预备方案稿与 settle 载体件在 open allow 面外，走 SPEC-024 v1.2 落差节同款显式绕行两笔（bypass 台账在档），事由零粉饰。
- 测试环境偏差：tempdir 跨进程可见性迟滞窗口以 run_stable 稀疏重试承接，处置先例承 SPEC-024 同参形条款环境项，parser 测试头注如实申报。
- 未实装申报偏差：calllogtool sqlite 索引腿承 SPEC-025 A6 零新增未实装，tally assemble 与 locator vectors 子命令等装饰面缺席，各 bin 头注落差节如实登记，候后批。

## 明确不做与后件

- 腿四 MCP 分派面 registry 接线与 mcpline 收编让位下批，方案预备稿 leg4-registry-wiring-design.md 已落 state/plan 候升格。
- 腿六长尾约 8000 行与进程外插件桥实装候后批。
- 围堰零改动全程维持；零新增 Cargo 依赖承 A6。
