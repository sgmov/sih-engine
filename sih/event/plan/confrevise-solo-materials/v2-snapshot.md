# confmodel 置信度数学模型推导档：置信度经济六要件公理化与常数三通道出生表

> 批：confmath-solo（pk-073 建模批，2026-09-07，单线 solo，零 sih-engine 写入零台账实装）
> 令源：用户 2026-09-07 令「置信度数学模型先行，你出任务提示词和任务包」，即 pk-073 出泊条件之立项裁落位；主会据以出任务包与委外提示词，本批委外执行。
> 上游：pk-073 泊材料（停泊事件 e785481b，六要件三轮对话用户裁定照录）；GOV-002 v2.3 既裁面；lease CONTRACT 修订四十一／四十二账单实装面。
> 批性质：零代码、零台账实装、零数值终裁——本批只给置信度经济立数学模型与常数候选表，一切数值呈用户裁；阶段二件（台账与抢占实装）只在文档面出现。
> 升版：v2 confconst-solo（2026-09-07）承用户裁「我不拍板数字，所有常数都要有数学模型支撑」——常数候选表呈裁形态废止，重构为三通道出生形（归一通道／推导通道／标定通道，§7）；公理组 A 与 B 逐字节零改动；v1 全文见 git 2cd22f7 与 §12 版本史。

## 1. 公理化口径 {#caliber}

- 公理组 A 即 pk-073 照录六要件，程序切片逐字节内嵌（生成器 build_confmodel.py 随批材料，切片自链上泊材料 JSON），禁手打转述。
- 公理组 B 即在役既裁补面，切片自 GOV-002 v2.3 节与 lease CONTRACT 修订四十一／四十二行，同样程序切片。
- 处置形承 constmodel-derivation-2026-09-05.md 四形判据：承接（数学仓已有条目覆盖语义给引用锚）、现推（从公理自行推导）、定义化（约定条文如实声明禁伪装推导）、呈裁（候选值列依据行呈人节点）。本批新事物是算子与不变量形式化，不是值级推导——一切数值走常数候选表呈裁。（v2 升版注：呈裁形自 confconst-solo 升版废止，数值出生改走 §7 三通道，四形判据中呈裁形退场，其余三形不变。）
- 载体纪律：先全读 sih-math/llm-friendly-build/mapping.md 再定位概念 ID；引用只落书单及图闭包；数学仓无对应的新概念显式申报提案态。

## 2. 公理组 A：六要件照录 {#axioms}

> 以下六条为 pk-073 链上泊材料 context 字段程序切片，逐字节照录零改写。

**A1（照录）**：一即置信度即信用点即支付货币，单一台账按正身开户、追加流水上链、余额即支付能力、历史即信誉曲线、余额下限零、欠账未清不得抢占、支付 fail-closed

**A2（照录）**：二即铸币即上链工作，干净收约铸币与主动链笔小额与修复他因事故铸币、自产自修不铸币、纯机械触发器零主观打分、数值走常数定义化通道

**A3（照录）**：三即罚金重于铸币（用户已裁准）：罚金与损害量级挂钩，事故级罚金须大于若干次清洁铸币之和，防高产能莽人刷分通道

**A4（照录）**：四即抢占制闭环：付置信度不退、得一裁事实与理由、放锁落链、被抢方修复按实据报销或无责回滚、余款充公铸给主动上链者

**A5（照录）**：五即阶段一账单不追溯即 grandfather

**A6（照录）**：六即人节点置信度语义（用户 2026-09-06 澄清原话要点）：人节点照常计分但只有提醒零强制，能被引擎管理的人见提醒即修正，不修正乃至负分者人类权责最高击穿一切机械锁，此类归现实世界管理，agent 只补救后果，与结算批第八节人类置信度评分条款不矛盾两裁各表，第八节为引擎内治理纪律、本轮为提醒不强制与越界归属

## 3. 公理组 B：既裁补面照录 {#axioms-b}

> 以下四条为在役档程序切片（GOV-002 v2.3 节行与 lease CONTRACT 修订四十一／四十二行），逐字节照录零改写。

**B1（照录）**：锁面定价先记账后算分即阶段一零置信度依赖

**B2（照录）**：开工锁少与首次临时加锁免费

**B4（账单事件与冻结常量照录）**：四类事件 bill_session_start（开工面计费按 allow 路径数）/ bill_lock（首次临时加锁免费 EXPANSION_FREE_QUOTA=1 内每会话一次后续逐路径计费 LOCK_BILL_UNIT=1）/ bill_close_unused（收工未用锁罚按 locked_paths \ used_paths 件数 UNUSED_LOCK_MULTIPLIER 倍）

**B5（阶段一处置照录）**：零余额零结算零评分（阶段一分阶段既裁，置信度台账后行时消费账单事件），grandfather 激活前行为零追溯

**B6（实跑点值照录）**：（open_face_bill 3 pts + lock_free 0 pts + lock_charged 1 pt）

**B3（候选方向照录）**：模型候选方向照录：类股票市场规则含涨停跌停概念。

## 4. 记号与载体锚 {#notation}

- 台账 L(t)：追加式事件序列，事件 e = (ts, kind, account, amount, ref)，ref 挂链上事件哈希（流水上链，A1）。追加式与版本化外化语义承 ORD-019（mapping.md:196，版本偏序与外化状态存储——台账形态参照，非本批新推导）。
- 账户域 A：开户函数 open: K → A 由正身核验记录诱导，账户键 k = core_hash（identity 契约修订八：六核心键去包装确定性哈希，跨报告配对键；identity_hash 缺省盐随机逐报告不同，不作账户键）。流水逐笔携 identity_hash 保追溯（租约会话与 facet 材料先例同形）。
- 余额 b: A × Time → ℕ，量纲信用点（pts）。
- 人席位账户集 H ⊆ A，机械强制算子域 A_mech = A \ H（D11）。

## 5. 定义与算子 {#defs}

**D1（余额与守恒，承 PROB-016）**：b_k(t) = Σ_{e∈L(t), acct(e)=k} amt(e)，amt: E → ℤ（铸币正、支付与罚金负）。总供给 M(t) = Σ_k b_k(t)。每事件守恒式 Σ_k Δb_k(e) = mint(e) − burn(e)；即余额是事件集上带符号计数测度的分账求和，可加性恒等式 I2 可机械回放核对。锚：PROB-016 计数测度与可加性（mapping.md:211）。

**D2（余额下限零）**：b_k(t) ≥ 0（A1 照录"余额下限零"的值域化），值域 ℕ 的偏序结构承 ORD-001 偏序集（mapping.md:165）。

**D3（支付算子，fail-closed 全化）**：pay(k, δ) := 若 [b_k(t) ≥ δ 且 O_k = ∅（D8）且 k ∈ A_mech] 则 b_k ← b_k − δ 并返回 ok；否则拒绝且余额零变动（I5）。guarded subtraction on ℕ——部分算子经守卫全化，fail-closed 即守卫失败落拒不动状态。支付算子对余额序单调：b ≤ b′ ⟹ pay 结果序保持（被拒形持平、成功形同减 δ），单调算子语义承 ORD-004 monotone operator（mapping.md:162）。

**D4（铸币算子，A2 形式化）**：mint(k, c)，触发谓词 T(c) ∈ {clean_close（干净收约），active_pen（主动链笔小额），repair_foreign（修复他因事故）}，纯机械触发零主观打分（A2 照录）；数额 c = f(kind) 为常数表步进函数（§7 三通道表），自产自修事件类不触发（A2 照录）。供给面：铸币是唯一正 mint 源。

**D5（罚金算子与防刷分不等式，A3 形式化）**：penalty(k, D)，D 为损害量级测度（事件分级：轻级/事故级，阈值槽位 D_acc）。罚金函数 p(D) 单调非降，且事故级下界公理：p(D) ≥ N_min · m_clean 对一切 D ≥ D_acc。防刷分论证（现推）：设单结算窗单账户最大清洁铸币次数上界 n_max，刷分净收益上界 = n_max·m_clean − p(D) ≤ (n_max − N_min)·m_clean；故 N_min > n_max ⟹ 事故刷分通道净负。即 A3 的形式化即参数不等式 N_min > n_max，方向由公理钉死；N_min 数值出生走 §7 推导通道（公式 N_min := n_max* + 1），n_max 经标定通道出 n_max*（v2）。

**D6（涨停跌停——窗变动界机制，B3 候选方向形式化）**：对结算窗 w 与账户 k，净变动 Δ_b(k,w) = Σ_{e∈w, acct=k} amt(e) 受界：|Δ_b(k,w)| ≤ U（对称带候选形一）或 Δ_b(k,w) ∈ [−U⁻, U⁺]（分段带候选形二），两形二择属机制规则面——机制选择归人裁规则非数值（v2）。超界变动递延下一窗（递延队列，先进先出）。机制语义：涨界防单窗暴铸，跌界防单窗清零（可恢复性）。参数 U、U⁻、U⁺ 走 §7 标定通道（程序 S3）；递延队列纪律随机制形裁定。锚：有界变差机制在数学仓无对应条目，显式申报提案态（§9）。

**D7（欠账与抢占门槛，A1 推论）**：欠账集 O_k := 账户 k 的未决结算事件集（挂起的 settlement 或分期罚金记录；余额下限零使欠账不体现为负余额，体现为未决义务记录）。抢占门槛公理化：O_k ≠ ∅ ⟹ pay(k, ·) 拒（A1 照录"欠账未清不得抢占"）。

**D8（抢占闭环算子，A4 形式化）**：preempt(k₁, target, δ_p)：
1. 即付：pay(k₁, δ_p) 成功即托管 escrow ← δ_p，**不退性**：escrow 不因后悔或撤回退回（A4 照录"付置信度不退"）；
2. 裁决：终局函数 settle 以得一裁事实与理由为输入（A4 照录），放锁落链（锁态变更走链上事件）；
3. 分配：被抢方修复按实据报销 r ∈ [0, δ_p]（实据面核验），escrow − r 为余款；无责回滚形 r = 0 且锁态回滚，托管全额转余款；
4. 充公：余款入公共池账户 P（sink 账户，非人非 agent），P 按机械触发器（主动链笔事件）铸给在链主动者（A4 照录"余款充公铸给主动上链者"）。
约束回放校验：r ≤ δ_p（I6 附件），escrow 生命周期逐笔可对账。

**D9（置信度语义，二择一显式裁定）**：
- 候选甲（取）：c_k(t) := b_k(t)——置信度与余额同一量的两个名字。依据：A1 照录原文"置信度即信用点即支付货币"，同名直取是公理的原样读法，零新增公理。历史即信誉曲线（A1 照录）即余额轨迹 (t, b_k(t))。（v2 升版：甲案自模型裁定升格归一通道定案，见 §7 表。）
- 候选乙（不取，登记提案态）：c = φ(b) 经单调映射。须新增公理定 φ（单调性、标定、界），本批公理组无此公理，如实登记为无公理支撑提案，不冒充已立。PROB-005 贝叶斯更新（mapping.md:155）登记为乙案未来的语义参照面（若信誉曲线需要加权信念解释时另批），本批不消费。

**D10（人节点账户，A6 形式化）**：人席位 h ∈ H 照常计分（D1 同一求和），但机械强制算子域排除：pay/penalty/preempt 的主体域为 A_mech = A \ H；对人的唯一作用面是视图提醒（viewer alarms 语义）。负余额倾向与不修正不触发任何机械强制，治理归现实世界（A6 照录"人类职责最高穿透一切机械锁"），模型内即 H 上无强制算子、仅观测与提醒。与结算批第八节人类置信度评分条款两裁各表（A6 照录原文声明不矛盾）。

**D11（阶段一 grandfather，A5 + B5 形式化）**：模型纪元 T₀（归一通道定案：台账上线日；上线日由阶段二批的启用链上事件定义即生效，事件定义值非人选数，见 §7 表）。求和域限定：b_k(t) = Σ_{e∈L(t), acct(e)=k, ts(e) ≥ T₀} amt(e)。T₀ 前账单事件（含 B6 实跑 3/0/1 pts）零余额贡献（I7）。billwire 实跑点值地位显式裁定：**grandfather**（A5 与 B5 照录直接覆盖），实跑值仅作常数表的量级参照（§7 依据行），不作模型常数的推导输入，不静默改数。

## 6. 不变量组（可机械检验，逐条回放判真伪） {#invariants}

| 编号 | 不变量 | 检验形 |
|---|---|---|
| I1 | b_k(t) ≥ 0 ∀k,t | 事件流回放逐步核对 |
| I2 | 守恒式 Σ_k Δb_k(e) = mint(e) − burn(e) 逐事件成立 | 回放核对，M(t) 与分账和零偏差 |
| I3 | 事故级罚金 p(D) ≥ N_min·m_clean | 回放核对（参数化，N_min 出生见 §7 推导通道；未标定期间参数门控休眠） |
| I4 | |Δ_b(k,w)| ≤ U 或 Δ_b(k,w) ∈ [−U⁻, U⁺] 且递延队列零溢出 | 回放核对（两形随机制裁，参数出生见 §7 标定通道） |
| I5 | 被拒支付零余额变动（fail-closed） | 回放核对 |
| I6 | O_k ≠ ∅ 的 pay/preempt 拒绝率 100%；报销 r ≤ δ_p | 回放核对 |
| I7 | T₀ 前事件零余额贡献 | 回放核对 |
| I8 | 人席位 H 上无强制算子触发记录 | 回放核对（A6） |

无脚本形态：每条不变量给定任一事件流 L 与参数即可逐条手验，真伪二元可判——单锚可验证性的承载面。

## 7. 常数三通道出生表（v2 升版，呈裁形态废止） {#constants}

> 升版令源：用户 2026-09-07 裁「我不拍板数字，所有常数都要有数学模型支撑」。支撑的充分形态不是依据行（v1 形），是出生通道：每个数值从下列三通道之一出生，人零挑选。人节点收窄后的职责面：裁模型升版、裁标定程序（程序即规则）、裁机制形与启停时刻；数值产出零人手。

### 7.1 三通道定义

- **归一通道（闭位）**：单位、纪元、命名约定。模型内定义即成立，无经验内容——选单位如选米尺，是定义行为非数值断言。
- **推导通道（公式闭位）**：值由公理加定理唯一给出，证明行与复算式随槽位；输入未落位时公式在档、值不虚设，输入落位即机械重算（重推导条款）。
- **标定通道（程序闭位、值开位）**：估计程序为规则面冻结在档（零参数序统计量形），数值由确定性程序对标定数据集机械产出；程序同参双跑逐字节一致、显式给参、零随机零读钟；触发条件显式在档。估计程序本身的修订是规则修订归人裁；数值产出零人手。

### 7.2 全槽位三通道表（呈裁残留零）

| 槽位 | 通道 | 值或公式 | 出生依据（证明行） |
|---|---|---|---|
| q 首免配额 | 归一（既裁照录） | 1 | B4 契约冻结常量 EXPANSION_FREE_QUOTA=1 定义化照录 |
| LOCK_BILL_UNIT | 归一（既裁照录） | 1 | B4 契约冻结常量定义化照录 |
| UNUSED_LOCK_MULTIPLIER | 归一（既裁照录） | 1 | B4 契约冻结常量定义化照录 |
| m_clean 清洁收约铸币额 | 归一 | := 1 pt | 单位锚定义：信用点单位定义为一次干净收约铸币额，选单位非经验断言；与 LOCK_BILL_UNIT=1 契约单位重合，双单位合一零自由度 |
| T₀ 模型纪元 | 归一 | := 台账上线日 | D11 纪元定义；上线日由阶段二批启用链上事件定义生效（事件定义值，非人选数） |
| c := b 置信度语义 | 归一（D9 甲案定案） | c_k(t) := b_k(t) | A1 同名直取零新增公理；v2 自模型裁定升格归一定案 |
| m_active 主动链笔铸币 | 推导 | = m_clean = 1 pt | 证明行：D1 整性 amt ∈ ℤ；A2 铸币存在正性 m_active ≥ 1；A2「小额」在整网格上仅存非空形式化 m_active ≤ m_clean（若取 m_active < m_clean 则整网格强制 m_active ≤ 0，与铸币存在矛盾，故形式化被唯一化非挑选）；归一 m_clean = 1 ⟹ m_active = 1。复算式：m_active = min{n ∈ ℕ⁺ : n ≤ m_clean} = m_clean |
| N_min 事故罚金下界倍数 | 推导（输入挂标定） | := n_max* + 1 | 证明行：A3 ⟹ N_min > n_max（D5 防刷分不等式）；n_max 为窗内单账户清洁铸币次数上界，经验量经标定通道出 n_max*；严格不等式最小整解 N_min = n_max* + 1 无自由度。复算式：N_min = ⌊n_max*⌋ + 1。重推导条款：n_max* 每次标定升版机械重算。过渡态：n_max* 未标定期间 N_min 零值断言，I3 参数门控休眠——机构一致于 B5 阶段一零结算零罚金，休眠是设计态非缺口。差异申报：任务包草拟式 ⌊U⁺/m_clean⌋ + 1 需铸币毛流入递延语义方成立（净界不界毛铸币，反例证明行见 7.4），毛递延登记 §9 提案四；提案立则改式 ⌊U⁺/m_clean⌋ + 1 机械重推导 |
| n_max* 窗内清洁铸币次数上界 | 标定 | 程序 S1，值待触发 | 估计程序：标定数据集 C 上每（账户，w-窗）清洁铸币事件计数取最大序统计量（零参数）。触发：账单数据攒量与 pk-074 子项一联动 |
| w 结算窗宽 | 标定 | 程序 S2，值待触发 | 估计程序：C 内每账户相邻清洁收约事件间隔序列取中位数（序统计量，零参数），向上取整到整日；整日为账本自然历法单位（trail 按日切文件）。触发同上 |
| U⁺ / U⁻ / U 窗变动界 | 标定 | 程序 S3，值待触发 | 估计程序：C 上每（账户，w-窗）净变动正部最大值为 U⁺、负部绝对值最大值为 U⁻、U := max(U⁺, U⁻)（对称形一参数）；D6 两形二择属机制规则面，形一参数即 S3 输出。触发同上 |
| D_acc 事故级损害阈值 | 标定（接口申报态） | 程序骨架 S4，数据面未落 | 损害测度操作化字段待 pk-074 子项一数据面落位；冻结承诺：估计程序取序统计量零参数形随数据面升版冻结；不虚构数据形 |
| m_repair 修复他因铸币分档表 | 推导上界＋标定分档 | 上界 m_repair(D) ≤ D；分档表随 D_acc | 上界证明行：A2「修复他因事故铸币」补偿性读法＋A4 报销语义类比（I6 r ≤ δ_p 同构），超 D 铸币即合谋铸币通道；解释步显式申报非公理暗改。分档边界标定通道随 D_acc 数据面冻结 |

### 7.3 标定程序确定性（F-3 承载）

S1/S2/S3 的程序文本（规则面）：输入为显式给参的事件集 C（jsonl，字段 ts/kind/account/amount）；第一步 S2 对每账户取相邻 clean_close 事件间隔（按日截断）全序列的中位数并上取整得 w；第二步按 w 从数据集最早时间戳起切窗，S1 取每（账户，窗）clean_close 计数的最大值，S3 取每（账户，窗）净变动（Σ amount）正部最大值 U⁺、负部绝对值最大值 U⁻；全程仅排序、计数、中位数、最大值、整除取整，零随机零读钟零环境量。参考实现 estimator_ref.py 随批材料，合成夹具同参双跑 cmp 逐字节一致在档——夹具输出值是确定性证明件，不是模型常数断言，不进本表不进任何实装位。真值产出归触发后继批。

### 7.4 净界不界毛铸币（反例证明行，N_min 草拟式缺口申报）

设窗内账户 k 铸 n 次清洁币并同窗受事故罚金 p。净变动 Δ = n·m_clean − p 受 I4 界只约束差值：n·m_clean 与 p 可同步增大而不破 |Δ| ≤ U⁺，故 ⌊U⁺/m_clean⌋ 不上界毛铸币次数 n_max。铸币毛流入递延语义（超界部分对铸币毛额递延，非对净额递延）下方有 n_max ≤ ⌊U⁺/m_clean⌋；该语义是 D6 的扩张，登记 §9 提案四，不暗改机制文本。

### 7.5 零裸常数核验（v2）

上表覆盖本档全部数值槽位（编号与日期除外）：归一通道值全部是定义或既裁照录，推导通道值全部挂证明行与复算式，标定通道全部挂零参数程序与触发条件；呈裁形态自本版废止。v1 候选表由本表替代，v1 全文见 git 2cd22f7 与 §12 版本史。

## 8. 与 pk-074 参数槽位对表 {#pk074}

- pk-074 子项一（超宽阈值调优）：与 D_acc、窗界族同属标定通道参数域，本批立槽位与零参数程序与触发条件，值由触发后继批机械产出，两泊件联动不冲突。
- pk-074 子项二（未用罚口径精确化）：UNUSED_LOCK_MULTIPLIER 在本档归一通道照录，精确化批若改值走常数表升版，模型面无冲突。

## 9. 新概念提案态显式申报 {#proposals}

以下概念数学仓 mapping.md 无对应条目，本档只作提案态登记，不冒充已立，走数学仓自身演化程序：

1. **窗变动界机制（涨停跌停形式化）**：有界变差带与递延队列的复合结构（D6）。
2. **托管结算语义（escrow）**：即付不退、终局分配、余款充公的三段算子（D8）。
3. **欠账即未决义务集**：余额非负约束下欠账从负余额转义务记录的表示（D7）。
4. **铸币毛流入递延语义**：窗界从净变动界扩张到铸币毛额界的 D6 扩张（§7.4）；立则 N_min 推导式改 ⌊U⁺/m_clean⌋ + 1 机械重推导。

## 10. 停批申报 {#settlement}

- **公理与实跑点值冲突申报**：无不可调和冲突。B6 实跑 3/0/1 pts 属阶段一账单事件，A5/B5 grandfather 照录直接覆盖其地位（零追溯），实跑值降为量级参照，张力消解于 D11 显式裁定。
- **数值零改动申报**：本批零 sih-engine 写入，契约冻结常量三件照录零改动；全部候选值未落任何实装位。（v2 续：本升版零新数值断言入实装位——m_active = 1 与 N_min 公式是既有公理加归一的推导输出，复算式在档；夹具输出值是确定性证明件非模型断言。）
- **语义二择一申报**：D9 甲案（c := b）依 A1 原文裁定为模型立场（v2 升格归一定案）；乙案登记提案态。若裁乙案，本档 §5 D9 升版，常数表增 φ 标定槽位。
- **差异申报（v2）**：任务包草拟的 N_min 推导式以 U⁺ 为输入，本版证明该式依赖毛流入递延语义（§7.4 反例），故现行式以标定通道 n_max* 为输入、U⁺ 式挂提案四重推导条款——差异是证明驱动的修正非暗改，草拟与定稿两式并在档。
- **升版申报（v2）**：呈裁形态废止是用户 2026-09-07 裁定的规则执行；公理组 A 与 B 逐字节零改动（对 v1 git 2cd22f7 可 diff 验证）；本升版自身零人挑数值。
- **checkmath**：本批产出只落 sih-math/docs/，mapping 与 entries 与 INDEX 零触碰。

## 11. 参考索引 {#reference}

- 数学仓消费面：sih-math/llm-friendly-build/mapping.md（全读）：PROB-016（mapping.md:211）、ORD-001（mapping.md:165）、ORD-004（mapping.md:162）、ORD-019（mapping.md:196）、PROB-015（mapping.md:205，抢占排队负载面前瞻槽位登记，本批不推导）、PROB-005（mapping.md:155，乙案提案语义参照，本批不消费）
- 条目实文：probability/entries/PROB-016-counting-measure-and-additivity.md、order/entries/ORD-001、ORD-004、ORD-019
- 仓内先例：sih-math/docs/constmodel-derivation-2026-09-05.md（四形判据与定义化通道）、facetmath-derivation-2026-09-04.md（检验判据式）
- 要件源：sih-engine/sih/state/parking/materials/pk-073.json（链上泊材料，程序切片源）
- 既裁面：sih-engine/doc/governance/GOV-002-mainline-lock-v1.md v2.3 节；sih-tools/lease/CONTRACT.md 修订四十一／四十二
- 制度约束：AGENTS.md 数学仓地位与唯一桥梁、既裁约束「引擎数学常数须有数学模型支撑」（v2 严格化：人不挑数，数值从模型通道出生）、工程基线一二四五条
- 升版令源与任务包：sih-engine/sih/state/plan/confconst-solo.md（2026-09-07）

## 12. 版本史 {#versions}

- **v1（confmath-solo，2026-09-07）**：六要件公理化初版，常数候选表十一槽位逐值携依据行呈用户裁；一裁 m-confmodel-1 stable_clear 终签 2384af26；sih-math 侧提交 2cd22f7。
- **v2（confconst-solo，2026-09-07，本版）**：承用户裁「我不拍板数字，所有常数都要有数学模型支撑」——常数表重构三通道出生形（§7），呈裁废止；m_active 推导定值、N_min 公式化挂标定输入、标定通道零参数程序三件（S1/S2/S3）与接口申报一件（S4）；差异申报与反例证明行在档（§7.4、§10）；提案四新增；公理组零改动。一裁 m-confconst-1。
