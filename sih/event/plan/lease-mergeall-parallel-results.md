# lease-mergeall-parallel 批结果档

## 结算要点

- 批名 lease-mergeall-parallel（并联形；stem 甲表认领 mergeall:new，概念锚 zh 全量融回，code 形显式申报无承，终裁登记候人节点）
- 会话 56add6536a910d30 双仓，意图笔 0da14bc2，src 与 tests 两锁；SPEC-025 正典先行（T6 三步绿）
- 令源用户 2026-09-14 goal 令全量融回加验收加严令（SDD/TDD 级联与 rmcp 重启实测，任务包验收补节 G1-G5）
- 并联三簇子代理并行施工（簇A 治理例行三件约 476 万 token、簇B T6 管线两件约 295 万、簇C 插件骨架约 117 万），主线验收与结算

## 首战交付（SPEC-025 腿一全加腿二前两件加腿五骨架）

1. 簇A：attnanchor 加 critsweep 加 gauge 三 bin（围堰 2820 行核心链移植），落差逐件头注申报（gchart 加 gqueue 加 contrib 加 sddgate 第四维加一旗域件形未移植等）。
2. 簇B：formatter 加 nomenclator 两 bin（围堰 942 行），落差各六条申报（register 写面未实装等），真实包双跑逐字节一致（formatter 对 GOV-003 报文与 nomenclator check 加 query 加 map 全一致）。
3. 簇C：插件槽位骨架即 tools_registry lib 模块（ToolProvider trait 四面加 ToolRegistry 加 ToolError 七态加 PluginManifest 严校验加 ExternalPluginBridge A4 缺席申报）加 lib.rs 导出加 registrydemo 演示 bin，在役 MCP 分派面零触碰（腿四公共施工面保全，接线让位后批）。

## 验收对表（G1-G5 逐项）

- G1 级联：SPEC-025 正典先行；工作区 AGENTS.md 工具层静态审计节增引擎融回位声明；围堰 CONTRACT 冻结零触碰。
- G2 TDD：主验收复跑新测 22 全绿即簇A 9 加簇B 7 加簇C 6；lease 全族 32 测回归不破；红转绿轨迹簇A 五笔与簇C 编译迭代如实登记（含两笔测试自身断言错修正即 bin 对测试错）。
- G3 同参双跑：formatter 加 nomenclator 真实包逐字节一致（簇B 实测）；gauge read 双跑 history 序列事件哈希与值逐一对表一致（d5cf7229 加 85df89d0 值 0.0 双侧同源），报文 pretty 形差异已在落差清单申报；attnanchor 时变字段出参以金向量测试论证。
- G4 rmcp 重启：归并重建后执行，实测三连（initialize 加 tools/list 加只读工具调用）与双旧进程收敛单进程，实录见本档补记。
- G5 槽位在役：registrydemo 实跑零退出，HeartbeatProvider 元数据与在役分派面逐字节对表。

## 队形验证

并联形三簇一次成立：三子代理独立施工零改派，主线验收一次过（复跑零新增红），落差申报制保对表忠实。

## 偏差

- 出参形偏差：五 bin 出参为 pretty 形而围堰为紧凑形，JSON 语义等价，SPEC-025 A1 对表面落差已在各 bin 头注申报。
- 施工分工偏差：三簇子代理并行写同一工地不同文件，git 暂存与提交归主线单点，承 SPEC-025 边界节分工形。
- 未实装申报偏差：进程外插件桥与 sihmcp 分派面 registry 接线与围堰若干子命令面未实装，SPEC-025 A4 与各 bin 头注落差节如实登记，候后批。

## 明确不做与后件

- parser 加 locator（腿二余）加腿三七件加腿四 MCP 收编加腿六长尾约 8000 行让位后批，按 SPEC-025 腿序。
- 进程外插件桥实装与 sihmcp 分派面 registry 接线候后批（A4 申报缺席）。
- 围堰 sih-tools 源码零改动全程维持；零新增 Cargo 依赖承 A6。
